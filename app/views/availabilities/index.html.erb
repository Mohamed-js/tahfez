
<h1>Availabilities</h1>

<table>
  <thead>
    <tr>
      <th>Teacher</th>
      <th>Time</th>
      <th>Day</th>
      <th>Occupied</th>
      <th>Student</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @availabilities.each do |availability| %>
      <tr>
        <td><%= availability.teacher_id_id %></td>
        <td><%= availability.time %></td>
        <td><%= availability.day %></td>
        <td><%= availability.occupied %></td>
        <td><%= availability.student_id_id %></td>
        <td><%= link_to 'Show', availability %></td>
        <td><%= link_to 'Edit', edit_availability_path(availability) %></td>
        <td><%= link_to 'Destroy', availability, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Availability', new_availability_path %>
